import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RequistPage } from './requist';

@NgModule({
  declarations: [
    RequistPage,
  ],
  imports: [
    IonicPageModule.forChild(RequistPage),
  ],
})
export class RequistPageModule {}
